version: "3.8"
services:
  education-app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules  # Prevents overwrite of node_modules in container
    command: npm run dev
